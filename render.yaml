services:
  - type: web
    name: fuelpro-manager
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    branch: main
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
